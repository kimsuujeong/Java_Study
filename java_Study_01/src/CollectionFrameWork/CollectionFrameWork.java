package CollectionFrameWork;

public class CollectionFrameWork {
	/* 기본 배열의 단점
	 * 1. 길이가 정적이다 (변화가 없으므로 불특정한 다수 객체 저장이 어렵다.)
	 * 2. 수정, 삭제가 조금 곤란하다.
	 * 
	 * List, Set, Map
	 * 1. List
	 * - 중복 저장이 가능한 순서가 있는 컬렉션
	 *  (인덱스가 존재한다*/
}
